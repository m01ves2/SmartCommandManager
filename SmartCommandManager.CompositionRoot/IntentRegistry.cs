namespace SmartCommandManager.CompositionRoot
{
    internal class IntentRegistry
    {
        //var intentRegistry = new IntentRegistry();

        //// FileSystem
        //intentRegistry.Register(CopyIntentDescriptor.Descriptor);
        //intentRegistry.Register(ListIntentDescriptor.Descriptor);

        //// Core
        //intentRegistry.Register(ExitIntentDescriptor.Descriptor);
        //intentRegistry.Register(HelpIntentDescriptor.Descriptor);

        //// Thread
        //intentRegistry.Register(PsIntentDescriptor.Descriptor);

        //services.AddSingleton(intentRegistry);
    }
}
